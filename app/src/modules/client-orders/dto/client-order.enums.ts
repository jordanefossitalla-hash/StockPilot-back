export enum ClientOrderDeliveryStatusDto {
  TO_DELIVER = 'TO_DELIVER',
  DELIVERED = 'DELIVERED',
}

export enum ClientOrderPriorityDto {
  LOW = 'LOW',
  NORMAL = 'NORMAL',
  HIGH = 'HIGH',
}
